import React from 'react';

/**
 * SEO компонент для управления мета-тегами
 * В Next.js мета-теги управляются через metadata в layout.tsx,
 * но этот компонент может использоваться для динамических SEO данных
 * Например, для structured data, Open Graph тегов и т.д.
 */
export const SEO: React.FC = () => {
  // В Next.js App Router SEO обычно управляется через metadata в layout.tsx
  // Этот компонент можно использовать для дополнительной SEO логики
  return null;
};
