'use client';

import { AdminHelpTooltip } from '@/shared/ui/admin/AdminHelpTooltip';
import { AdminHelpInfoButton } from '@/shared/ui/admin/AdminToolbarIconButton';

import styles from './WaybillsRulesInfoTip.module.css';

const HELP = {
  title: 'Как работать с путевым листом',
  note: 'Список заданий водителю: планирование доставок, отметки о выполнении и привязка к договорам. Есть два режима просмотра — таблица и календарь. Подходит ответственным менеджерам и водителям.',
} as const;

const VIEW_ITEMS = [
  'Переключатель «Таблица | Календарь» в шапке меняет способ отображения (выбор запоминается).',
  'Таблица — подробный список за период «Дата от / Дата до»; по умолчанию открывается неделя с сегодняшнего дня.',
  'Календарь (на компьютере) — весь месяц сразу: компактные карточки заданий и смены водителей внизу дня.',
  'В календаре наведите курсор на задание — увидите детали (время, заказчик, доставка, грузчики и кто платит). Клик открывает карточку; «+» в дне создаёт задание на эту дату.',
  'Даты в таблице и карточках показываются как дд.мм.гг; в превью смен — как дд.мм.',
] as const;

const DAY_ITEMS = [
  'В режиме таблицы «Дата от» и «Дата до» задают период списка.',
  'В режиме календаря период равен выбранному месяцу — листайте стрелками «‹ ›» над сеткой.',
  'Блок «Когда можно планировать доставки» показывает доступность водителей на ближайшую неделю (в таблице).',
  'Путевой лист на день желательно заполнить до 08:00. Правки после 08:00 подсвечиваются меткой «после 08:00».',
  'Чипы статуса («В плане», «Выполнено», «Не выполнено») сужают и таблицу, и календарь; «Все» показывает полный набор.',
] as const;

const CREATE_ITEMS = [
  '«+ Задание» — создать строку: время, направление, текст задания, заказчик, стоимость доставки/грузчиков и кто платит.',
  'Поиск по номеру договора подставляет ФИО, адрес и телефон; при необходимости их можно поправить вручную.',
  'Назначьте водителя со схемой смен — появятся доступные дни; выходные, отпуск и больничный выбрать нельзя.',
  'Назначьте ответственного и водителя — задание появится у водителя в разделе «Мой маршрут».',
  '«Копировать» — быстрая копия задания на другую дату; оригинал не меняется (статус можно сменить вручную).',
] as const;

const STATUS_ITEMS = [
  'Водитель (или ответственный) отмечает «Выполнено» после доставки.',
  'Если доставка не состоялась — «Не выполнено» с обязательной причиной. Строку не удаляйте.',
  'Удалить («в корзину») можно только задание «В плане», и только тот, кто его вписал.',
  'Через 30 дней после перемещения в корзину задание удаляется безвозвратно.',
  'Выполненные задания удалять нельзя.',
  '«В план» возвращает выполненное или невыполненное задание обратно в работу.',
] as const;

const DRIVER_ITEMS = [
  'Раздел «Мой маршрут» — задания водителя на выбранный период (по умолчанию неделя вперёд).',
  'В списке удобно позвонить клиенту и открыть адрес на карте.',
  'Отметку о выполнении водитель ставит сам — ответственному не нужно править таблицу за него.',
] as const;

const RULES_ITEMS = [
  'Избегайте узких временных окон (например, только с 17:00 до 18:00) — лучше «по созвону за час».',
  'Планируйте доставки с учётом уже стоящих на день и смен водителей, без больших разрывов и наложений по времени.',
  'Направление (двери, бавария и т.п.) помогает быстро понять тип выезда.',
  'В календаре инициалы внизу дня показывают смену: зелёный — доступен, серый — выходной, оранжевый — отпуск/больничный.',
] as const;

export function WaybillsRulesInfoTip() {
  return (
    <AdminHelpTooltip
      title={HELP.title}
      align="center"
      panelClassName={styles.helpPanel}
      body={
        <div className={styles.body}>
          <p className={styles.note}>{HELP.note}</p>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Таблица и календарь</p>
            <ul className={styles.list}>
              {VIEW_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Период и фильтры</p>
            <ul className={styles.list}>
              {DAY_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Создание задания</p>
            <ul className={styles.list}>
              {CREATE_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Статусы и отметки</p>
            <ul className={styles.list}>
              {STATUS_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Мой маршрут</p>
            <ul className={styles.list}>
              {DRIVER_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Правила планирования</p>
            <ul className={styles.list}>
              {RULES_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>
        </div>
      }
    >
      <AdminHelpInfoButton title={HELP.title} aria-label={HELP.title} />
    </AdminHelpTooltip>
  );
}
