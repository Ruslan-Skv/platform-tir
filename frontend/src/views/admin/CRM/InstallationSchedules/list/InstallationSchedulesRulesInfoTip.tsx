'use client';

import { AdminHelpTooltip } from '@/shared/ui/admin/AdminHelpTooltip';
import { AdminHelpInfoButton } from '@/shared/ui/admin/AdminToolbarIconButton';

import styles from './InstallationSchedulesRulesInfoTip.module.css';

const HELP = {
  title: 'Как работать с графиком монтажей',
  note: 'Расписание выездов монтажников: один общий список с фильтром по направлениям, режимы таблицы и календаря. Можно привязать мастера из справочника и пакет договора с заказ-нарядом либо заполнить поля вручную.',
} as const;

const VIEW_ITEMS = [
  'Переключатель «Таблица | Календарь» меняет способ отображения (выбор запоминается в браузере).',
  'Таблица — подробный список за период «Дата от / Дата до»; по умолчанию открывается неделя с сегодняшнего дня.',
  'Календарь (на компьютере) — весь месяц: компактные карточки монтажей. Наведите курсор — увидите детали; клик открывает карточку; «+» в дне создаёт монтаж на эту дату.',
  'На телефоне календарь скрыт: показываются карточки списка с кнопкой звонка заказчику.',
] as const;

const FILTER_ITEMS = [
  'Чипы направления («Окна», «Двери», «Потолки» и т.д.) оставляют записи одного направления; «Все» показывает полный набор. Ремонт — в отдельном разделе «План-график ремонта».',
  'Чипы статуса («В плане», «Выполнено», «Не выполнено») сужают и таблицу, и календарь.',
  'Быстрые периоды «Сегодня / Неделя / Месяц» и поля «Дата от / Дата до» задают диапазон списка.',
  'В режиме календаря период равен выбранному месяцу — листайте стрелками «‹ ›» над сеткой.',
] as const;

const CREATE_ITEMS = [
  '«+ Монтаж» — создать запись: дата, время (или свободный текст времени), направление, монтажник, договор/заказ-наряд, заказчик.',
  'Монтажника выберите из справочника «Мастера» (фильтр по направлению) или включите «Ввести монтажника вручную».',
  'Если у мастера в справочнике привязан аккаунт, монтаж появится у него в разделе «Мои монтажи».',
  'Пакет документов ищется по номеру договора, заказчику или названию: подставляются ФИО, адрес и телефон — их можно поправить.',
  'После выбора пакета доступны заказ-наряды из пакета (с учётом назначенных мастеров). Можно указать заказ-наряд и номер договора вручную без привязки к пакету.',
] as const;

const STATUS_ITEMS = [
  '«Выполнено» — монтаж выполнен; комментарий необязателен.',
  '«Не выполнено» — с обязательной причиной. Запись не удаляйте.',
  '«Перенести» меняет дату у той же записи и возвращает её «В план».',
  'Удалить («в корзину») можно только запись со статусом «В плане» — она исчезнет из списка, но доступна в корзине.',
  'В шапке иконка корзины открывает список удалённых записей; оттуда можно восстановить монтаж на его дату.',
  `Через 30 дней после перемещения в корзину запись удаляется безвозвратно.`,
  '«В план» возвращает выполненную или невыполненную запись обратно в работу.',
] as const;

const RULES_ITEMS = [
  'Фильтруйте по направлению, чтобы не смешивать окна, двери, потолки и другие выезды в одном просмотре.',
  'Направление «Ремонт» ведётся отдельно в разделе «План-график ремонта» — там длинные договоры с еженедельной динамикой.',
  'Если мастер уже выбран в пакете документов — удобнее брать заказ-наряд из списка пакета; иначе используйте ручной ввод.',
  'Время лучше указывать реалистично: интервал «с–по» или текст вроде «к 10.00» / «по созвону».',
  'В календаре цвета карточек соответствуют статусу: синий — в плане, зелёный — выполнено, красный — не выполнено.',
  'О создании, правках, выполнении и невыполнении монтажей приходят уведомления в колокольчик (автору записи, ответственному менеджеру пакета и мастеру — если у него в справочнике привязан аккаунт) и на внешние каналы из настроек уведомлений.',
] as const;

export function InstallationSchedulesRulesInfoTip() {
  return (
    <AdminHelpTooltip
      title={HELP.title}
      align="center"
      panelClassName={styles.helpPanel}
      body={
        <div className={styles.body}>
          <p className={styles.note}>{HELP.note}</p>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Таблица и календарь</p>
            <ul className={styles.list}>
              {VIEW_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Период и фильтры</p>
            <ul className={styles.list}>
              {FILTER_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Создание монтажа</p>
            <ul className={styles.list}>
              {CREATE_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Статусы и отметки</p>
            <ul className={styles.list}>
              {STATUS_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Правила планирования</p>
            <ul className={styles.list}>
              {RULES_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Мои монтажи</p>
            <ul className={styles.list}>
              <li>
                Раздел «Мои монтажи» — монтажи мастера на выбранный период (по умолчанию неделя
                вперёд), если у карточки мастера привязан аккаунт.
              </li>
              <li>
                В этом разделе можно отметить «Выполнено» или «Не выполнено»; планирование и перенос
                — только в общем графике.
              </li>
            </ul>
          </section>
        </div>
      }
    >
      <AdminHelpInfoButton title={HELP.title} aria-label={HELP.title} />
    </AdminHelpTooltip>
  );
}
