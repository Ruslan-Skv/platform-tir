export const repairTemplateEstimate = `
<div class="docPrint">
  <h1>Смета (расчёт заказа)</h1>
  <p>Комментарий из вкладки «Данные»:</p>
  <pre class="estimatePre">{{estimate.notes}}</pre>
</div>
`.trim();
