'use client';

import { AdminHelpTooltip } from '@/shared/ui/admin/AdminHelpTooltip';
import { AdminHelpInfoButton } from '@/shared/ui/admin/AdminToolbarIconButton';

import styles from './EstimatesListRulesInfoTip.module.css';

const HELP = {
  title: 'Как работать с расчётами',
  note: 'Список сметных расчётов для оформления договоров: поиск, очереди, группировка по объектам и действия со строками. Подходит менеджерам и смежным ролям.',
} as const;

const SCOPE_ITEMS = [
  '«Мои» — расчёты, которые вы создали, или привязанные к договорам, где вы ответственный менеджер.',
  '«Все» — полный список в рамках прав доступа к разделу; фильтр «Менеджер» доступен только здесь.',
  'Менеджерам при первом заходе удобнее область «Мои», чтобы не путаться в большом общем списке.',
] as const;

const PIPELINE_ITEMS = [
  '«В работе» — актуальные расчёты для оформления и ведения сделок.',
  '«В перспективе» — отложенные варианты; можно вернуть обратно во «В работе».',
  'Счётчики на вкладках показывают, сколько расчётов в каждой очереди.',
] as const;

const FILTER_ITEMS = [
  'Поиск — по названию расчёта, заказчику и адресу объекта.',
  '«По объектам» / «Плоский список» — группировка по адресу или все расчёты подряд.',
  'Даты и менеджер — дополнительная фильтрация (менеджер — в режиме «Все»).',
  '«На странице» — сколько строк показывать; листание внизу таблицы.',
] as const;

const VIEW_ITEMS = [
  '«По объектам» — тёплая карточка объекта, внутри по «+» / «−» связанные расчёты.',
  'Расчёты без адреса показываются отдельными карточками со скруглением.',
  '«Плоский список» — каждый расчёт отдельной карточкой, без группировки.',
] as const;

const WORK_ITEMS = [
  'В строке: перспектива/работа, архив, редактирование, копирование, разделение сметы, корзина.',
  'Наценка (%) — доп. процент к позициям; у объекта действует на все расчёты адреса в группе, у расчёта — только на него.',
  '«+ Новый расчёт» открывает рабочую область; «из замера» — создать расчёт по выполненному замеру.',
  'Архив скрывает расчёты из основного списка и из выбора при оформлении договоров; корзина — для удаления с возможностью восстановления.',
] as const;

const BINDING_ITEMS = [
  'Привязка к договору отображается в колонке использования; у подписанного договора или Д/с правка и удаление могут быть недоступны.',
] as const;

const LINKED_SPLIT_ITEMS = [
  'Зачем: один полный расчёт по объекту нужно оформить разными договорами (или Д/с) — каждый связанный экземпляр получает свой набор позиций сметы, без дублей.',
  'Подготовка: сохраните исходный расчёт со сметой (есть позиции), укажите адрес объекта. Без адреса и без сохранённых позиций связанные копии недоступны.',
  'Шаг 1 — создать связанные экземпляры: в строке исходного расчёта нажмите «Копировать» (иконка копирования). В модалке выберите не «Обычную копию», а один из вариантов связанной.',
  '«Связанная копия — начать связку» (если расчёт ещё не в связке): создаёт первый связанный экземпляр и открывает связку разделения. После сохранения у каждого расчёта связки отметьте состав в «Разделении сметы».',
  '«Связанная копия — в эту же связку» (если связка уже есть): добавляет ещё один экземпляр в текущую группу; позиции распределяются между всеми расчётами связки.',
  '«Связанная копия — новая связка»: отдельная группа на том же объекте (другой набор договоров / другая смета). Исходный расчёт остаётся в своей связке (или становится якорем новой, если раньше связки не было).',
  '«Связанная копия — в другую связку на объекте»: если по адресу уже есть другие связки — выберите нужную в списке и добавьте туда экземпляр.',
  'Обычная копия — отдельный независимый расчёт без связи при разделении; для деления сметы на договоры её не используйте.',
  'Шаг 2 — разделить позиции: в строке каждого связанного расчёта (вкладка «В работе») нажмите «Разделение сметы» (иконка разведения стрелок). Отметьте позиции, которые войдут в этот экземпляр для договора, и сохраните.',
  'В модалке видно: полную сумму сметы, сумму выбранного здесь и остаток нераспределённых позиций по всей связке. Позиции, уже занятые другим связанным расчётом, повторно выбрать нельзя.',
  'Повторите разделение для каждого экземпляра связки, пока нужные позиции не распределены. Затем каждый экземпляр можно прикрепить к своему договору в пакете документов.',
  'На объекте может быть несколько независимых связок. В списке «По объектам» связанные расчёты видны внутри карточки адреса; у участников связки отображается бейдж «Связка 1/3»: первое число — номер связки на объекте, второе — текущая позиция этого расчёта среди участников связки (не «номер по порядку создания»).',
  'Номера во втором числе пересчитываются: новая связанная копия вставляется сразу после того расчёта, с которого копировали. Если копировали со «второго», новый станет «1/3», а бывший третий сдвинется в «1/4». При добавлении или перестановке в списке бейджи у соседей могут измениться.',
  'Бейдж «Связка …» подсвечивается салатовым, когда все позиции сметы уже распределены по расчётам связки (в «Разделении сметы» ничего не осталось невыбранным). Пока есть нераспределённые позиции — обычный (нейтральный) цвет бейджа.',
  'Ограничения: в архиве связанные копии недоступны; при подписанном договоре или Д/с правка и разделение блокируются. Если в связке уже есть копии — сначала сохраните состав позиций в «Разделении сметы» у текущего расчёта, иначе добавить ещё одну связанную копию в ту же связку нельзя.',
] as const;

export function EstimatesListRulesInfoTip() {
  return (
    <AdminHelpTooltip
      title={HELP.title}
      align="center"
      panelClassName={styles.helpPanel}
      body={
        <div className={styles.body}>
          <p className={styles.note}>{HELP.note}</p>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Область списка</p>
            <ul className={styles.list}>
              {SCOPE_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Вкладки</p>
            <ul className={styles.list}>
              {PIPELINE_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Фильтры</p>
            <ul className={styles.list}>
              {FILTER_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Вид списка</p>
            <ul className={styles.list}>
              {VIEW_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Работа с расчётом</p>
            <ul className={styles.list}>
              {WORK_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Привязки к договорам</p>
            <ul className={styles.list}>
              {BINDING_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>

          <section className={styles.section}>
            <p className={styles.sectionTitle}>Связанные расчёты и разделение сметы</p>
            <ul className={styles.list}>
              {LINKED_SPLIT_ITEMS.map((item) => (
                <li key={item}>{item}</li>
              ))}
            </ul>
          </section>
        </div>
      }
    >
      <AdminHelpInfoButton title={HELP.title} aria-label={HELP.title} />
    </AdminHelpTooltip>
  );
}
